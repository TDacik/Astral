(* This module groups constructors for builtin inductive predicates.
 *
 * Author: Tomas Dacik (idacik@fit.vut.cz), 2024 *)

let loc_ls = LS.loc_ls
let loc_dls = DLS.loc_dls
let loc_nls = NLS.loc_nls

let struct_ls = LS.struct_ls
let struct_dls = DLS.struct_dls
let struct_nls = NLS.struct_nls

let mk_pto_ls = LS.mk_pto
let mk_pto_dls = DLS.mk_pto
let mk_pto_nls = NLS.mk_pto

let mk_freed = Freed.mk

let mk_ls = LS.mk
let mk_dls = DLS.mk
let mk_nls = NLS.mk
